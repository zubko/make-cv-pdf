// @flow
import * as React from 'react';
import { View, Text } from '@react-pdf/renderer';
import styled from '@react-pdf/styled-components';

export default () => (
  <View>
    <Text>Skill Item</Text>
  </View>
);
